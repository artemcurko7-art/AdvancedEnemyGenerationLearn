using UnityEngine;
using static UnityEngine.GraphicsBuffer;

public class BombardierEnemy : Enemy
{
    public override void Initialize(Transform target)
    {
        base.Initialize(target);
    }
}
